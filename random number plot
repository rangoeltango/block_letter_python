#Snippet showing import alias, plotting, and random number range
from matplotlib import pyplot as plt
import random

numbers_a = range(1,20)
numbers_b = random.sample(range(1000),19)
plt.plot(numbers_a,numbers_b)
plt.show()
